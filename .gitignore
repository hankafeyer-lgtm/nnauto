.env
.env.*
!.env.example

# dependencies
node_modules/

# builds
dist/
build/
.next/
out/

# logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# local tooling
.vscode/
.idea/

# OS
.DS_Store

# misc
cookies.txt

# generated reports (only root-level reports folder)
/reports/

# Cursor / local assets
attached_assets/
.next/

# Local DB backups (contain sensitive data — keep only on this machine)
backups/

# Generated / temp files
*.tsbuildinfo
listings.json
next
rest-express@1.0.0
test-results/

# Local-only temp scripts (may contain SSH keys / secrets — never commit)
.tmp_*.exp
.tmp_*.sh
.tmp_ssh.exp
.tmp_check.sh

# Watermark preview renders (dev-only, regenerate via scripts/watermark-preview.mjs)
public/watermark-preview/
.backups/
